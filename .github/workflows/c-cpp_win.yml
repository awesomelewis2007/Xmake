name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: Compile
      run: |
        cd Xmake
        cl Xmake.cpp function.cpp help.cpp Global.cpp 
